version: '3.8'

services:
  mysql-db:
    image: mysql:8.0
    container_name: kindergarten_mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: "dev_kms_2024"
      MYSQL_DATABASE: kindergarten_db
    ports:
      - "3306:3306"
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/01_init.sql
      - mysql_data:/var/lib/mysql
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-pdev_kms_2024"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  mysql_data:
    driver: local
